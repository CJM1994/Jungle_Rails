<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <%= render 'item_table' unless enhanced_cart.length == 0 %>
  <%= render 'empty' unless enhanced_cart.length > 0 %>

  <%= render 'stripe' unless enhanced_cart.length == 0 %>

</section>
